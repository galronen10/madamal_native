import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  buttonsContainer: {
    alignContent: 'center',
    alignItems: 'center',
    flexDirection: 'row',
  },
  container: {
    alignItems: 'center',
    justifyContent: 'center',
    paddingBottom: 0,
  },
  image: {
    height: 160,
    width: 240,
  },
});
